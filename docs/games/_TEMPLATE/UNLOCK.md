# {Game Title} — Unlock integration

**Requires:** `{requires-unlock-id}`  
**Earned by clearing:** `{previous-game-id}`  
**Reward code (shown on hub after clear):** `{REWARD-CODE}`  
**Unlocks next:** `{next-game-title}` via Terminal `LOGIN {REWARD-CODE}`

## Hub

Entry in `assets/js/games.js` — `status: "in-production"` until launch.

## Terminal

Code registered in `UNLOCK_CODES` in `kids-unlocks.js`.

## Win UI copy

Short hint: "Type LOGIN {REWARD-CODE} in Terminal Trainer"
